Fixed DNS integer fields (Zone TTL, SOA refresh/retry/expire/serial/minimum, record TTL) to support the full RFC-compliant unsigned 32-bit range (0–4294967295); also fixed a bug where a record TTL of 0 was treated as unset.
