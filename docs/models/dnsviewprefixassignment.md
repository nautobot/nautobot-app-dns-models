# DNS View Prefix Assignment Model

The DNS View Prefix Assignment is the through model linking a `DNSView` to an IPAM `Prefix`. Each `(dns_view, prefix)` pair is unique.

- `dns_view` (DNSView): The DNS View.
- `prefix` (Prefix): The IPAM prefix associated with the View.
